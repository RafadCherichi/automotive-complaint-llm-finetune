# Phase 1: data prep + eval harness only (per blueprint.md Section 7).
# No torch/training deps here — those only run on Colab/Kaggle GPUs.
pandas
requests
jsonschema
tqdm

# Tokenizer-only (no torch, no model weights, CPU text processing) — used by
# scripts/check_seq_lengths.py to check real token-length distribution against
# Qwen3's tokenizer before locking Phase 2's max_sequence_length. Not a training
# dependency: no GPU use, no model loading, just vocabulary/BPE text splitting.
transformers
tokenizers>=0.22.0,<=0.23.0
safetensors  # verifying downloaded adapter weight files (parses tensor headers, no GPU/torch needed)

# Phase 4: local demo, calling Ollama's REST API (Ollama itself is a separate native
# app install, not a pip package — see docs/blueprint.md). No torch/GPU deps added here;
# Ollama does the model inference out-of-process, Streamlit just calls its HTTP API.
streamlit
